<h1>投稿を編集する</h1>
<%= form_with model: @evaluation, url: public_evaluation_path(@evaluation.id), method: :patch, local:true do |f| %>

  <p>曲名</p>
  <%= f.text_field :music_title %>

  <p>ポイント（1から10で選んでね）</p>
  <%= f.select :point,*[1..10] %>

  <p>感想</p>
  <%= f.text_area :evaluation %>

  <p><%=f.submit "変更する" %></p>

  <% end %>
